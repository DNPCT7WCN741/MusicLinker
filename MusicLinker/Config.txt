import Foundation

/// ⚠️ 敏感配置文件 - 请勿提交到 Git
/// 这个文件用于存储 API Key，应该加入 .gitignore
/// 
/// 使用说明：
/// 1. 填写下面的 API Key
/// 2. 确保这个文件在 .gitignore 中（已配置）
/// 3. 永不提交到版本控制

struct AppConfig {
    /// DeepSeek API Key - 从 https://platform.deepseek.com 获取
    /// ⚠️ 仅在此处填写，不要硬编码到其他地方
    static let deepseekAPIKey: String = "sk-a0a67393e9be4f1a85ecded922968b89"
    
    /// 是否启用 AI 歌词分析
    static let isAIEnabled: Bool = true
}
